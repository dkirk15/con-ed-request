{
  "$schema": "./log-alert.schema.json",
  "service": "api-server",
  "source": {
    "format": "json",
    "stream": "stdout"
  },
  "match": {
    "levelAtLeast": 50,
    "event": "auth.clerk_provisioning_lookup_failure"
  },
  "aggregation": {
    "count": 5,
    "windowSeconds": 300,
    "groupBy": [
      "service",
      "event"
    ],
    "deduplicationKey": "event",
    "cooldownSeconds": 900,
    "alertEvent": "alert.auth.clerk_provisioning_lookup_failures"
  },
  "notification": {
    "recipient": "on-call-administrators",
    "cooldownSeconds": 900,
    "deduplicationKey": "event",
    "message": "Authentication provisioning outage: {{failureCount}} Clerk lookups failed in {{windowSeconds}} seconds. {{action}}",
    "includeFields": [
      "failureCount",
      "threshold",
      "windowSeconds",
      "cooldownSeconds",
      "action"
    ],
    "excludePaths": [
      "clerk",
      "req.headers.authorization",
      "req.headers.cookie",
      "res.headers.set-cookie"
    ]
  }
}