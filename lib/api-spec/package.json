{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check:report-errors": "node ./scripts/check-report-error-contract.mjs",
    "codegen": "pnpm run check:report-errors && orval --config ./orval.config.ts && pnpm --filter @workspace/api-zod run fix-generated-index && corepack pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "8.22.0"
  }
}
